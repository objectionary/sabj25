# SPDX-FileCopyrightText: Copyright (c) 2026 Objectionary.com
# SPDX-License-Identifier: MIT
---
name: benchmark
'on':
  push:
    branches:
      - master
    paths-ignore: [ 'README.md', '.github' ]
concurrency:
  group: benchmark-${{ github.ref }}
  cancel-in-progress: true
permissions:
  contents: write
  pull-requests: write
jobs:
  benchmark:
    timeout-minutes: 15
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: 25
      - uses: actions/cache@v5
        with:
          path: ~/.m2/repository
          key: ubuntu-jdk-25-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: ubuntu-jdk-25-maven-
      - run: mvn clean test --errors --batch-mode
      - run: .github/benchmark.sh
      - uses: peter-evans/create-pull-request@v8
        with:
          sign-commits: true
          branch: benchmark
          commit-message: 'new benchmark results'
          delete-branch: true
          title: 'New benchmarking results'
          assignees: yegor256
          base: master
